var _x = x;
var _y = y;

draw_circle_color(x, y, 10, c_black, c_black, false);

//if (ChatterboxIsStopped(chatterbox)) {
    //If we're stopped then show that
   // draw_text(_x, _y, "(Chatterbox stopped)");
//} else {
   //draw_text(_x + 200, _y, text);
//}