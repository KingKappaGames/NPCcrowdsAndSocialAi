if(live_call()) { return live_result }

draw_circle_color(x, y, influenceConsequenceRadius, c_red, c_maroon, false);

/*
draw_circle_color(x, y, influenceRadius, c_red, c_maroon, true);

draw_text(x, y, influence);